# Copy this file to .env.local and fill in real values. .env.local is
# gitignored — never commit real secrets.

# Powers the real Brand Dossier Generator (src/app/api/generate-dossier).
# Without this set, "Create Brand Dossier" gracefully falls back to the
# offline mock generator so the demo still works.
ANTHROPIC_API_KEY=
